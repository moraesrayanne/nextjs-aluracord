import { Box } from "@skynexui/components"
import styles from "../styles/Chat.module.css"

export default function Chat() {
    return (
        <Box className={styles.firstBox}>
        <h1 className={styles.temp}>Página de chat</h1>
        </Box>
    )
}